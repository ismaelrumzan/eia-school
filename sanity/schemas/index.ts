import page from "./page-schema";
import { landingPageType } from "./landing-page-schema";
import { heroBlockType } from "./hero-block-schema";

const schemas = [page, landingPageType, heroBlockType];

export default schemas;
